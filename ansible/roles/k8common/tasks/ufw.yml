---
- name: UFW - Allow common k8 connections
  community.general.ufw:
    rule: allow
    comment: "{{ item.name }}"
    proto: "{{ item.proto }}"
    to_port: "{{ item.port }}"
  loop: "{{ k8common_ufw_rules }}"
