apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: heimdall-pvc-config
  namespace: heimdall
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
