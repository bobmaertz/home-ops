apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homer-app
  namespace: argocd
spec:
  destination:
    namespace: homer
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    repoURL: 'https://github.com/bobmaertz/home-ops.git'
    targetRevision: HEAD
    path: kubernetes/orion/manifests/default/homer/
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
